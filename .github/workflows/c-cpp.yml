name: C/C++ CI

on:
  push:
    branches: []
  pull_request:
    branches-ignore: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # Install dependencies
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-13 g++-13 cmake libgtest-dev make
        sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-13 100
        sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-13 100

    # Build Google Test
    - name: Build GTest
      run: |
        mkdir -p build/gtest
        cd build/gtest
        cmake /usr/src/gtest
        cmake --build .

    # Configure
    - name: Configure CMake
      run: |
        cmake -B build \
          -DENABLE_COVERAGE=ON \
          -DCMAKE_BUILD_TYPE=Debug \
          -DGTEST_ROOT=build/gtest

    # Build
    - name: Build
      run: cmake --build build

    # Upload build artifacts
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-dir
        path: build

  run:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - uses: actions/checkout@v4

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-dir
        path: build

    # Run program (placeholder)
    - name: Run program
      run: |
        chmod +x build/plazza || true
        ./build/plazza 1 5 2000
      # will fail automatically if exit code != 0

  test:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - uses: actions/checkout@v4

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-dir
        path: build

    # Run unit tests
    - name: Run tests
      run: |
        chmod +x build/plazza_tests
        cd build
        ./plazza_tests

  coverage:
    runs-on: ubuntu-latest
    needs: test

    steps:
    - uses: actions/checkout@v4

    # Install coverage tools only
    - name: Install coverage tools
      run: |
        sudo apt-get update
        sudo apt-get install -y gcc-13 lcov
        sudo update-alternatives --install /usr/bin/gcov gcov /usr/bin/gcov-13 100

    - name: Download build artifacts
      uses: actions/download-artifact@v4
      with:
        name: build-dir
        path: build

    # Run tests again to generate .gcda
    - name: Run tests for coverage
      run: |
        chmod +x build/plazza_tests
        cd build
        ./plazza_tests

    # Generate coverage and enforce 80%
    - name: Generate coverage
      run: |
        cd build

        lcov \
          --gcov-tool gcov-13 \
          --capture \
          --directory . \
          --ignore-errors mismatch,empty,source \
          --output-file coverage.info

        lcov \
          --remove coverage.info '*/test/*' '*/tests/*' '*/CMakeFiles/*' '/usr/*' '*/_deps/*' \
          --ignore-errors unused \
          --output-file coverage_filtered.info

        lcov --summary coverage_filtered.info

        COVERAGE=$(lcov --summary coverage_filtered.info | awk '/lines/{gsub("%","",$2); print $2}')
        echo "Total coverage: ${COVERAGE}%"

        if (( $(echo "$COVERAGE < 80" | bc -l) )); then
          echo "❌ Coverage below 80%"
          exit 1
        fi

    - name: Generate HTML coverage
      run: |
        genhtml build/coverage_filtered.info --output-directory coverage_html --ignore-errors source

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      with:
        name: coverage-html
        path: coverage_html